// Replace any instances of util._extend with Object.assign
// For example:
// const util = require('util');
// const config = util._extend(defaultConfig, customConfig);

// Should be changed to:
// const config = Object.assign({}, defaultConfig, customConfig);